name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python3 -m unittest discover -s tests -v
      - run: python3 scripts/validate_repo.py
      - run: python3 -m py_compile skills/aft-gauntlet/scripts/compose_prompt.py scripts/install_skill.py scripts/validate_repo.py

  claude-plugin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - run: npx --yes @anthropic-ai/claude-code@2.1.226 plugin validate --strict .claude-plugin/plugin.json
      - run: npx --yes @anthropic-ai/claude-code@2.1.226 plugin validate --strict .claude-plugin/marketplace.json
